# Node dependencies & Build output
# =================================
/node_modules
/.next/

# Optional: Add other common ignores if needed
# .DS_Store
# *.log
# /coverage
# /.env.local
# /.env.development.local
# /.env.test.local
# /.env.production.local

# Firebase build and deployment files
/firebase-debug.log
/firebase-debug.*.log
.firebaserc

# Firebase Hosting
/firebase.json
*.cache
hosting/.cache

# Firebase Functions
# Note: If your functions directory also uses node_modules,
# ensure it's ignored relative to that directory, e.g., /functions/node_modules/
# Your existing entry looks correct IF your functions are in a 'functions' subfolder.
/functions/node_modules/
/functions/.env
/functions/package-lock.json

# Firebase Emulators
/firebase-*.zip
/.firebase/
/emulator-ui/

# Logs (your existing ones are good)
# *.log # You might already have this below
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Environment files (local configs)
# Your existing entry for /.env.* is good for files like .env.local etc.
/.env.*
.env

# Next.js build output
out/
build/
.next/
node_modules/